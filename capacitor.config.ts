import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.track.covid19',
  appName: 'track-covid19',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
